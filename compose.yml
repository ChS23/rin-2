services:
  calendar:
    container_name: rin-bot
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - bot_data:/app/data
    restart: unless-stopped

volumes:
  bot_data:
